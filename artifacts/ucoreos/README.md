# uCoreOS example index

The eight uCoreOS chapters each have a folder with a `coverage.json` record.
The chapter model is documented in
[`docs/kernels/ucoreos/chapters.md`](../../docs/kernels/ucoreos/chapters.md).

The chapter HTML reports are archived by hash rather than duplicated as large
showcase files in this directory.
