# StarryOS example index

StarryOS has completed strict coverage recordings, but the raw trace is too
large to commit as an interactive HTML example. App-level evidence is under
`apps/`, and subsystem evidence is under `showcase/`.

Use the evidence README for reproduction commands and artifact scope.
