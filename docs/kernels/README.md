# Kernel-specific documentation

Manifest authoring guidance and syntax live in `docs/manifests/`. Documentation
that only applies to one kernel lives under `docs/kernels/<kernel>/`; currently
the uCoreOS chapter matrix is the only document of that kind.
